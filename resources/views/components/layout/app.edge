<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title || "AdonisJS - A fully featured web framework for Node.js" }}</title>
  
  @if ($slots.meta)
    {{{ await $slots.meta() }}}
  @endif

  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body>

  <div class="max-w-3xl mx-auto mt-6">
    @include('partials/nav')

    {{{ await $slots.main() }}}
  </div>

</body>
</html>
